{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter @wyzetalk/db build && pnpm --filter @wyzetalk/web build"
  },
  "deploy": {
    "staticDir": "dist"
  }
}
