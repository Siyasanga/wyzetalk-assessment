{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "build": {
      "commands": ["pnpm --filter @wyzetalk/db build", "..."]
    }
  },
  "deploy": {
    "startCommand": "npx serve -s dist -l $PORT"
  }
}
